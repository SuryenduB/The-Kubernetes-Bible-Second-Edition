resources:
  # --- AI & Language Learning ---
  - ai-language-learning.yaml          # ai-language-learning ns: backend, postgres, schema ConfigMap
  - media/openlingo.yaml                # openlingo ns: app, db (StatefulSet), netpols, PDB, ingress
  - linguacafe                          # linguacafe ns: webapp, mariadb, redis, netpols
  - base/ollama-deployment.yaml         # ai ns: ollama, openwebui, PVCs, ingress

  # --- Identity (SailPoint IdentityIQ stack) ---
  - iiq-stateful.yaml                   # iiqstack ns (all-in-one): iiq, ldap, activemq, db, db-mysql, mail, counter, ssh, phpldapadmin, init job, netpols, RBAC

  # --- Media ---
  - media                               # media ns: audiobookshelf, calibre-web (with importer sidecar)

  # --- Homepage Dashboard ---
  - homepage/homepage.yaml              # homepage ns: gethomepage deploy + ConfigMap + Tailscale svc

  # --- Monitoring ---
  - monitoring                          # monitoring ns: beszel hub + agent DaemonSet
